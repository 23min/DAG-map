// refine-coordinates-none.js — no-op coordinate refinement strategy.
// Positions remain unchanged. This is the current layoutMetro behavior.

export function refineCoordinatesNone(/* { nodes, positions, childrenOf, parentsOf } */) {
  // No refinement — positions stay as initially assigned.
  // This matches the current layoutMetro behavior.
}
